import './bootstrap';

// Architect UI theme — Bootstrap JS + responsive sidebar behaviour.
// jQuery, fancytree, jquery-ui, select2, metismenu are all loaded as
// synchronous globals in the HTML head (plain <script> tags).
import '../themes/architect/src/app';
